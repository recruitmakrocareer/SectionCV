{
  "name": "makro-spot-the-difference",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "build": "node scripts/build-site.cjs"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "jsdom": "29.0.0"
  }
}
